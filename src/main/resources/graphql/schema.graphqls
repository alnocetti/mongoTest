type Query {
    hello: String
    getUser(id: ID!): User
    getUsers: [User]
}

type Mutation {
    createUser(firstName: String!, lastName: String!): User
}

type User {
    id: ID
    firstName: String
    lastName: String
}